/**
 * @file
 * Dear mom behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.dearMom = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
